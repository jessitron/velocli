I just want to be able to substitute stuff in a single velocity template.

## build

`mvn package`

## run

`bin/velocli <template_file> [--parameter value]...`

(for whatever parameters and values, it doesn't care)

Lisence CC0, do what you want